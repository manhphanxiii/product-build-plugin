---
name: evals-gate
description: Derive and run a permanent product eval set, enforce a measurable ship gate, and block shipping on failures or regressions.
---

# Evals Gate

This is a pointer. The real skill lives in the skill-set repository that contains this file.
Resolve `<skill-set-root>` with `git rev-parse --show-toplevel` from the directory holding this file, then read `<skill-set-root>/skills/evals-gate/SKILL.md` and follow it exactly.
Resolve every relative link inside it against `<skill-set-root>/skills/evals-gate/`, not against this file.
