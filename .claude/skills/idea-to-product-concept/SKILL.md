---
name: idea-to-product-concept
description: Gather product evidence, interview the user once across all unresolved areas, and produce a traceable product concept.
disable-model-invocation: true
---

# Idea to Product Concept

This is a pointer. The real skill lives in the skill-set repository that contains this file.
Resolve `<skill-set-root>` with `git rev-parse --show-toplevel` from the directory holding this file, then read `<skill-set-root>/skills/idea-to-product-concept/SKILL.md` and follow it exactly.
Resolve every relative link inside it against `<skill-set-root>/skills/idea-to-product-concept/`, not against this file.
