---
name: to-prd
description: Synthesize the approved concept and prototype decisions into a PRD, architecture document, and finalized ADR history.
---

# To PRD

This is a pointer. The real skill lives in the skill-set repository that contains this file.
Resolve `<skill-set-root>` with `git rev-parse --show-toplevel` from the directory holding this file, then read `<skill-set-root>/skills/to-prd/SKILL.md` and follow it exactly.
Resolve every relative link inside it against `<skill-set-root>/skills/to-prd/`, not against this file.
