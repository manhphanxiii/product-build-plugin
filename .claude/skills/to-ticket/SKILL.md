---
name: to-ticket
description: Split an approved PRD and architecture into dependency-ordered vertical-slice tickets sized for one context window.
---

# To Ticket

This is a pointer. The real skill lives in the skill-set repository that contains this file.
Resolve `<skill-set-root>` with `git rev-parse --show-toplevel` from the directory holding this file, then read `<skill-set-root>/skills/to-ticket/SKILL.md` and follow it exactly.
Resolve every relative link inside it against `<skill-set-root>/skills/to-ticket/`, not against this file.
