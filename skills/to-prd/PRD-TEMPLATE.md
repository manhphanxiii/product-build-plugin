# <Product Name>

## Problem

Describe the user-visible problem and supporting evidence.
Quote verbatim from `client-note/` when customer language exists instead of paraphrasing it.

## Solution

Describe product behavior and boundaries without implementation detail.

## User Stories

1. As an `<actor>`, I want `<feature>`, so that `<benefit>`.

## Success Metrics

| Metric | Baseline | Target | Measurement |
|---|---|---|---|

## Out of Scope

List explicit exclusions.

Do not require persona fields.
Avoid file paths and code unless a prototype-derived snippet is the clearest exact expression of an approved decision.
